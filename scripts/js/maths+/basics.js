
/**
 * module pour les compléments en maths
 */


/**
 * get random numbers with intervall ([min,max[)
 * @param {int} min int number included
 * @param {int} max int number excluded
 * @returns 
 */
function randi(min, max) {
    return Math.random() * (max - min) + min;
}


//end page